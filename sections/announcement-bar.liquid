<div class="fill center gradient-accent" style="background:{{ section.settings.background | default: 'var(--accent-gradient)' }};color:{{ section.settings.color | default: 'var(--ink)' }}">
  {{ section.settings.text }}
</div>

{% schema %}
{
  "name": "Announcement Bar",
  "class": "announcement-bar-section gradient-accent",
  "settings": [
    {
      "type": "inline_richtext",
      "id": "text",
      "label": "Text",
      "default": "Welcome to our store! <a href=\"#\"> Find out more</a>"
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background"
    },
    {
      "type": "color",
      "id": "color",
      "label": "Text Colour"
    }
  ]
}
{% endschema %}
